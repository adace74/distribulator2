Run Command Syntax & Examples
-----------------------------

Syntax: run [ssh_flags] "command" on [server_group,server_group...] [reverse]

Description: Runs a given command across multiple servers,
             in either normal or reverse sort order.

Examples:
    run "uptime"            -- Runs uptime on current server group
                               in forward order.
    run "uptime" reverse    -- Runs uptime on current server group
                               in reverse order.
    run "uptime" on app,www -- Runs uptime on app and www server groups
                               in forward order.
    run "uptime" on app01   -- Runs uptime on app01.somewhere.com.
    run "uptime" on app01,www01
                            -- Runs uptime on app01.somewhere.com and
                               www01.somewhere.com in forward order.
    run "uptime" on app,www reverse
                            -- Runs uptime on app and www server groups
                               in reverse order.
    run -t "vi"             -- Runs vi on current server group,
                               in forward order, and forces an ssh tty.
    run -t "vi" on app,www  -- Runs vi on app and www server groups,
                               in forward order, and forces an ssh tty.

----------------------------------------------------------------------
$Id$
----------------------------------------------------------------------
